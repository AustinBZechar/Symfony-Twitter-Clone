{% extends 'base.html.twig' %}

{% block body %}
    <div id="postform">
        {{ app.session.get('username') }}, what you are doing?
        <br>
        {{ form(tweetForm) }}
        <div id="homeinfobox">
            {{ app.session.get('followers', 0) }} followers <br>
            {{ app.session.get('following', 0) }} following <br>
        </div>
    </div>
{% endblock %}
